# Python
__pycache__/
*.pyc
*.pyo
*.pyd

# Cache / temp
cache_data/
thumbs/
*.bak

# User settings
settings.json
settings_v*.json
*.local.json

# Private user data — must not be published
newfiles/
pkw_papers/
data/
samples/

# Database files: ignore everything in dbfiles/, then white-list only the two
# DBs that accompany the paper.
*.db
*.sqlite
*.sqlite3
!dbfiles/pkw_1.db
!dbfiles/raman.raman.db
!dbfiles/alldata.db
!dbfiles/CHECKSUMS.md5

# Generic binary/document types — opt-in publishing only
*.tif
*.tiff
*.png
*.jpg
*.jpeg
*.pptx
*.ppt
*.docx
*.xlsx
*.zip
*.pdf

# paper/ folder is held back for now (contains pre-publication manuscript
# material and specimen photographs). Re-enable on a per-file basis if
# needed for review.
paper/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Paper reproduction dataset (33 images) — published with the paper
!dataset/
!dataset/images/
!dataset/images/*.png

# Internal working notes / private revision tooling — not for public repo
checklist.md
context-notes.md
tools/rebuild_final_docx.py
